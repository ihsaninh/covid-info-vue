export const SET_DATA_GLOBAL = (state, dataGlobal) => {
  state.dataGlobal = dataGlobal;
};

export const SET_DATA_ID = (state, dataId) => {
  state.dataId = dataId;
};

export const GET_DETAIL_GLOBAL = (state, dataDetailGlobal) => {
  state.dataDetailGlobal = dataDetailGlobal;
};

export const GET_DETAIL_ID = (state, dataDetailId) => {
  state.dataDetailId = dataDetailId;
};

export const GET_NEWS = (state, dataNews) => {
  state.dataNews = dataNews;
};

export const GET_NEWS_PAGING = (state, dataNewsPaging) => {
  state.dataNewsPaging = dataNewsPaging;
};
